{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ListEje list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Codieje</th>
                <th>Nombeje</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('listeje_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.codiEje }}</td>
                <td>{{ entity.nombEje }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('listeje_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('listeje_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('listeje_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
