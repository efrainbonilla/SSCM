{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ListPais list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombpais</th>
                <th>Latpais</th>
                <th>Lngpais</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('region_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombPais }}</td>
                <td>{{ entity.latPais }}</td>
                <td>{{ entity.lngPais }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('region_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('region_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('region_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
